# this file was generated by DevGlider to initialize CI
triggers:
  pushes:
    branches:
      - "*"
  prs:
    branches:
      - "*"

# comment - hi hello
jobs:
  build:
    stages:
      - run: echo "hello build job"

  other-job:
    depends-on:
      - build
    stages:
      - run: echo "hello other job"
