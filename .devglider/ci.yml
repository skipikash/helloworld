# this file was generated by DevGlider to initialize CI
triggers:
  pushes:
    branches:
      - "*"
  prs:
    branches:
      - "*"

# comment 3
jobs:
  build:
    stages:
      - run: sleep 180

  other-job:
    depends-on:
      - build
    stages:
      - run: echo "hello other job"
